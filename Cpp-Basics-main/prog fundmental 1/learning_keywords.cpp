#include <iostream>//preprocessor directive(include): is used to import files from libraray.
//isostream fuction is uded to imoprt header files from librsrsy on ehich cout and cin rund
int main(){
    std::cout<<"hello world";//main()function tells the compiler from where to start execution of program
    //; semicolon marks the end od statement
    return 0;//tells compiler that the function is over.
}